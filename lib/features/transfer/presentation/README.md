# transfer/presentation/

| Item | Purpose |
|---|---|
| `bloc/` | `TransferBloc`: the whole flow's state. |
| `pages/` | `SendMoneyPage`. |
| `widgets/` | Confirmation sheet and success view. |
| `validation_messages.dart` | Turns the transfer validator error enums into the text shown under each field. |
