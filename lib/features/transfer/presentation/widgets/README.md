# transfer/presentation/widgets/

| File | What it does |
|---|---|
| `confirm_transfer_sheet.dart` | `showConfirmTransferSheet()`: the confirmation step (amount, recipient, account, note). It returns true when the user taps "Confirm and send". |
| `transfer_success_view.dart` | The success screen: check mark, amount, recipient, new balance, and Done. |
