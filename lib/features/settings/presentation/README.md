# settings/presentation/

| Item | Purpose |
|---|---|
| `bloc/` | `ThemeBloc`. |
| `pages/` | `SettingsPage`. |
| `widgets/` | `SettingsSection` and `SettingsRow`. |
| `theme_preference_ui.dart` | Maps `ThemePreference` to Flutter's `ThemeMode`, plus the label and icon for each segment. |
