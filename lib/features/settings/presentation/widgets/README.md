# settings/presentation/widgets/

| File | What it does |
|---|---|
| `settings_section.dart` | `SettingsSection` (a title in capitals above a rounded card of rows) and `SettingsRow` (icon, label, value or custom `valueWidget`, optional trailing action). |
