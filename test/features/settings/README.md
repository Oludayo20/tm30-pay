# test/features/settings/

| File | What it does |
|---|---|
| `theme_bloc_test.dart` | Applies and saves the new mode, ignores the current mode, keeps the mode when saving fails. |
| `settings_page_test.dart` | Profile, account and wallet data rows; offline data source; the Dark segment sends the event; Sync now refreshes the wallet. |
