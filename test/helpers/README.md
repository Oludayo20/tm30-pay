# test/helpers/

| File | What it does |
|---|---|
| `mocks.dart` | Mocktail mocks for every repository and bloc, a sample `testUser`, and `registerFallbacks()` for `any()` matchers. |
| `fixtures.dart` | Predictable test data: `fixedNow` (24 Sep 2026, 12:00), `tx(n)` and `txs(from, to)` transactions, `snapshot()`, `page()` and `receipt()`. |
